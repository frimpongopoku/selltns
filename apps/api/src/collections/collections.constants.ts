export const MAX_TAGS_PER_COLLECTION = 10;
export const MAX_TAG_LENGTH = 30;

// Collections are typically far fewer than products, so a smaller page —
// still enough to demonstrate infinite scroll on a bigger catalog.
export const COLLECTIONS_PAGE_SIZE = 12;
export const COLLECTIONS_PAGE_SIZE_MAX = 60;
